import { NationalContactPipe } from './national-contact.pipe';

describe('NationalContactPipe', () => {
  it('create an instance', () => {
    const pipe = new NationalContactPipe();
    expect(pipe).toBeTruthy();
  });
});
